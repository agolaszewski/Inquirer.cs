namespace Sandbox2
{
    internal class Program
    {
        private static void Main(string[] args)
        {
            //Question.Checkbox("Test", Enum.GetValues(typeof(ConsoleColor)).Cast<ConsoleColor>()).WithConfirmation().WithDefaultValue(ConsoleColor.DarkCyan).ConvertToString(x=> { return x.ToString(); }).Prompt();
        }
    }
}